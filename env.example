# Email Configuration for Contact Form
# Copy this file to .env.local and fill in your actual values

# Gmail Configuration
EMAIL_USER=your-gmail@gmail.com
EMAIL_PASS=your-app-password

# Alternative SMTP Configuration (comment/uncomment as needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password

# Instructions:
# 1. For Gmail: Enable 2-factor authentication and generate an app password
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate a new app password for "Mail"
# 4. Use that app password (not your regular Gmail password) in EMAIL_PASS
# 5. Make sure .env.local is in your .gitignore file 